/** Set to true to re-enable the event calendar across the app. */
export const CALENDAR_ENABLED = false;

/** Set to true to re-enable the Park Selector across the app. */
export const PARK_SELECTOR_ENABLED = false;
